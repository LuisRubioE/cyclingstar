{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm build"
  },
  "deploy": {
    "startCommand": "node apps/api/dist/tick/main.js",
    "cronSchedule": "0 */6 * * *",
    "restartPolicyType": "NEVER"
  }
}
