/**
 * apps/web: SPA React + Vite + Tailwind + TanStack Query + React Router (SPEC 12).
 * Paso 3: solo andamiaje. El arranque de Vite, el layout con cabecera y el cliente de API
 * tipado desde @cyclingstar/shared llegan en el Paso 8.
 */
export const WEB_PACKAGE = '@cyclingstar/web' as const
