# Dudas de los redactores (para la pasada de coherencia)

- [00-cabecera.md] El H1 del documento (`# El generador de recorridos: la gramática de motivos (diseño final)`) no lo emite mi fichero: lo nombro en prosa en la primera línea de la sección 0, porque el encargo pedía empezar por el `## 0.`; la pasada de coherencia tiene que colocar el H1 encima al ensamblar.
- [00-cabecera.md] HEAD al escribir la cabecera es `2b21ce7` (git rev-parse) y los mapas leyeron `8585ca2` del 2026-09-14 (mapa 04 l. 3); `ENGINE_VERSION` sigue en 69 en `constants.ts` l. 718. He citado `8585ca2` como el árbol contra el que se escribió, como manda §D; la coherencia debe decidir si el documento final cita ese HEAD o el de ensamblaje, y comprobar que ninguna línea citada se movió entre los dos.
- [00-cabecera.md] El esqueleto §B.1 sitúa la migración nueva en `packages/db/migrations/00NN_race_routes_kind.sql`, pero el directorio real es `packages/db/drizzle/` (última: `0039_el_recorrido_es_del_mundo.sql`, con `drizzle/meta/0039_snapshot.json`); en la cabecera solo digo «última migración 0039», pero las secciones 10, 11 y 15 deberían citar la ruta real.
- [00-cabecera.md] La frase «los perfiles son la ENTRADA de la calibración táctica» aparece en §A del esqueleto entre «», pero es prosa de agenda §4.18 l. 895, no cita del dueño; la he escrito sin comillas angulares (regla «citas del dueño entre «» y solo esas»). Lo mismo con «es media montaña con la etiqueta cambiada» (epics E3), que va en cursiva. La coherencia debe unificar ese tratamiento en las demás secciones.
- [00-cabecera.md] Longitud: 186 líneas envueltas a 100 columnas (el estilo de `docs/entrenamiento.md`), 21 de ellas la tabla del mapa del documento; sin envolver son 74 líneas físicas. El objetivo de §D eran 120: el exceso está en las diez líneas de §0.4, que llevan cifra y procedencia cada una; si hay que recortar, las filas de la tabla de §0.5 pueden acortarse sin perder nada.
- [01-diagnostico.md] Las cifras 72 (kind leído por stageKindOf contra kind declarado, mapa 06 §2.1) y 49 (etiquetas corregidas por stageHistory.ts::calendarStageSpec, l. 199) son dos medidas distintas y la sección las presenta así, sin afirmar que la diferencia se explique solo por la regla de 5 km; la sección 11 (real) debe usar el mismo encuadre y no presentar el 49 como subconjunto del 72.
- [01-diagnostico.md] Los mapas discrepan en algunas líneas de calendar.ts: la semilla de stagesFromEdition está en l. 221 (mapa 01 §0, datos §1.3) o l. 224 (mapa 02 §5 y §7); oneDaySpec ocupa l. 400-407 (esqueleto, arquitectura) o 400-408 (mapa 02); las ramas de buildRace son l. 899-929 (mapa 02 §6) o 902-924 (arquitectura §1.3). He usado 221, 400-407 y 899-929; la pasada de coherencia debe fijar una sola cita en todo el documento.
- [01-diagnostico.md] La sección tiene 245 líneas físicas pero con párrafos largos (hasta 1.300 caracteres por línea, como el esqueleto); si el documento final se envuelve a ~100 caracteres como docs/entrenamiento.md, la longitud real supera con holgura las 300 líneas objetivo y quizá convenga podar la tabla del orden de tiradas de mountainSegments (§1.1.2) o la tabla v63/v64 (§1.7) si el conjunto se pasa.
- [01-diagnostico.md] La sección cita `docs/tactica.md` R28.1(d) como origen del criterio «2.800 a 4.200» de desnivel mediano siguiendo la fila 22 del mapa 05 §10; no he leído tactica.md directamente, así que si otra sección cita ese criterio con otra procedencia (balance v60 §1b) hay que unificar.
- [01-diagnostico.md] La citación «el llano no es una recta» de la cabecera de profileGen.ts l. 3-5 está parafraseada (el mapa 01 §1 dice que la cabecera afirma que nunca es una línea recta); si la pasada de coherencia prefiere solo citas literales de código, sustituir por prosa sin comillas.
- [02-principios.md] `admite(geo, sk)` y `degradar(relieve)` los nombra la decisión 12 de §C.1 pero §B.1 no los lista entre los exports de `grammar/geo.ts`; los he declarado ahí con firma TypeScript. Además la cadena de degradación de la decisión 12 es `montana → media → ondulado → llano` (cuatro valores) mientras `Relieve` tiene cinco (`alta`); he escrito `alta → montana → media → ondulado → llano`. La sección 6 debe usar la misma firma y la misma cadena.
- [02-principios.md] El test de determinismo que pongo en `grammar/generate.test.ts` (misma `StageRequest` → mismo `GeneratedStage` bit a bit; `season` no mueve `arch.skeleton` ni los motivos `firma`) y el de pureza en `grammar/veto.test.ts` (espía sobre `sampleProfile` sin llamadas en 300 generaciones) son formulaciones mías; el esqueleto solo nombra los ficheros. La sección 15 (plan, pasos 5 y 6) debe recogerlos con ese mismo enunciado o sustituirlos por los suyos.
- [02-principios.md] Cito el comentario y el literal `routeSource: 'generado' as const` de `packages/db/src/raceRoutes.ts` como l. 49-52 (contadas sobre el fichero); `ingeniero.md` §2.6 lo cita como l. 48-51. La sección 11 debe usar una sola cifra.
- [02-principios.md] En la tabla 2.11 afirmo que `skeletons.test.ts` sella «ninguno a 0 ni > 60 % por clase y zona» sobre 300 semillas: es la banda de variedad de `geografia.md` §12 paso 3 (d) trasladada al esqueleto; la sección 13 (bandas de variedad) y la 15 (paso 4) deben confirmar el umbral 60 % o poner el suyo.
- [02-principios.md] He escrito que la regla «ningún `between(rand, a, b)` con literales en `grammar/`» es una regla de revisión, no un test; si la sección 15 quiere hacerla mecánica (un grep en CI) tiene que decirlo ella.
- [03-modelo.md] `RouteContext` y `DEFAULT_ROUTE_CONTEXT` no están en §B.2 del esqueleto (solo aparecen como argumento de `stageMix` en §B.1). Los he definido en `generate.ts` como { country: '', raceClass: '2', format: 'una-semana', season: BASE_SEASON, routeSource: 'generado' } (§3.7). La sección 7 (vueltas) y la 15 (plan, paso 1) deben usar exactamente esa forma y ese valor por defecto, o la pasada de coherencia debe unificar.
- [03-modelo.md] `Summary` (lo que devuelve `aggregate` en `routeCensus.ts`) no está definido en §B.2 ni en banco.md. Lo he definido en §3.10 como { n, cuantiles: Partial<Record<keyof RouteStats, {p10,p50,p90,min,max}>>, reparto: Partial<Record<keyof RouteStats, Record<string, number>>> }. La sección 13 debe escribir `ROUTE_CENSUS_TARGETS` sobre esas dos formas (cuantil o fracción de reparto).
- [03-modelo.md] Firmas decididas aquí y no en el esqueleto: `validateMotif(m: Motif): string | null`, `renderMotif(m: Motif, rand: () => number, geo: GeoSignature): Segment[]` (§3.2) y `composeTour(n: number, terrain: RouteTerrain, seedBase: string, ctx: RouteContext): StageSpec[]` (§3.6). Las secciones 4, 7 y 8 deben usar las mismas.
- [03-modelo.md] He fijado que `arch` NO se congela en `race_routes` (solo `profile`, `route_source`, `kind`, `label`, `time_trial`), apoyado en ingeniero §3.3 y el comentario de `schema.ts` l. 509; la ficha recompone `arch` con `stagesForSeason(raceId, season)`. Las secciones 10 y 11 (y la API en el paso 10) deben ser coherentes con eso.
- [03-modelo.md] Regla de agregado de `CalendarRace.routeSource` escrita en §3.11: `real` si todas las etapas son `real`, `generado` si todas son `generado`, `mixto` en cualquier otro caso. Las secciones 11 y 16 (`inventario-recorridos.mjs`) deben aplicar la misma regla.
- [03-modelo.md] Longitud: 407 líneas frente a las 330 objetivo. Los nueve bloques TypeScript obligatorios de §B.2 ocupan ~215 líneas por sí solos; la prosa son ~190. Si hay que recortar, §3.12 (nota de alias) y las viñetas de §3.3 son lo prescindible.
- [04-gramatica.md] Hueco de gramática no resuelto por §B.3: `ARCH.motivo.muro.g` con suelo 8 deja fuera como dificultad INTERMEDIA los bergs al [5; 8) % (Cauberg 1,2 × 5,8; Bosberg 1 × 6; Taaienberg 0,53 × 6,6; Polytechnique 0,78 × 6, mapa 07 §1.1 y §1.3), y choca con la fila pólder de geografia.md (muro [0,3; 1,0] × [5; 8]) que la sección 6 va a traducir a `ZONAS.flandes`. La sección lo documenta como hueco (§4.2 y §4.6 punto 4) y remite a la 17. La pasada de coherencia decide: o bajar `muro.g` a [5; 16] (un muro < 8 % es un `puerto` corto sin COL, `stageKindOf` sigue diciendo `clasica`) o aceptar el hueco y que las secciones 5, 6 y 17 lo digan igual.
- [04-gramatica.md] Mismo tipo de hueco con `ARCH.motivo.cota.g` techo 7: Civiglio 4,2 × 9,7 y Superga 4,9 × 9,1 no caben como cota intermedia (ni `muro`, km > 3). Documentado en §4.2 y usado honestamente en el ejemplo de Lombardía (§4.7, Civiglio con g recortado a 7,0). Alternativa para coherencia: `cota.g` [4; 10] dejando que la intersección por zona lo estreche.
- [04-gramatica.md] `muro_meta` con `cotaFinal.km` en (1,0; 2,2]: la sección declara a V16 el conjunto {puncheur, alto} (no solo `puncheur` como dice la decisión 7) porque mapa 03 §10.4 cita en `docs/motor.md` §12.1 una segunda vía a `alto` («últimos 3 km al ≥ 5 %») que un muro de 2,2 km al 14 % puede activar. Hay que verificar contra `finish.ts` l. 165-189 si esa vía existe tal cual y, si no, volver a {puncheur} en la tabla de §4.3, en la nota y en el test del paso 3.
- [04-gramatica.md] Constantes que esta sección necesita y que no están en la tabla de §B.3 (las declara en §4.5 y pide que la sección 12 las recoja): `ARCH.motivo.cadena.hijos` [2; 8], `ARCH.motivo.cadena.enlace` [1,5; 6], `ARCH.motivo.descenso.km` [2; 25], `ARCH.motivo.expuesto.km` [5; 60], `ARCH.motivo.circuito.maxHijosShare` 0,8. Las cuatro primeras salen de arquitectura §3.1; la quinta es juicio de esta sección.
- [04-gramatica.md] Semilla de detalle de los hijos de un `circuito`: la sección escribe `dib|raceId|stageIndex|season|slot|hijo{h}|i{intento}` (posición de `hijo{h}` antes del intento) y dice que la lista cerrada es de la sección 8. La sección 8 debe confirmar o corregir esa posición y que `renderMotif` reciba una fábrica `randDe(h)` para los hijos.
- [04-gramatica.md] `Motif.forma`: la sección decide que `progresiva` = `climb` tal cual, `regular` = mismas rampas barajadas por `rand`, `irregular` = progresiva + rampa de `rampaIrregular` en la mitad alta. No lo fija ninguna propuesta ni §C; si la sección 8 o la 12 prefieren otra implementación de `regular`, cambiar aquí.
- [04-gramatica.md] `cotaFinal` en TODO `MetaKind` con subida (siete de nueve) y `Motif.km` como total del motivo (aproximación + subida + bajada + valle); `sector_meta` con su sector en `hijos[0]`. Es la lectura que esta sección hace de §B.2 (`cotaFinal?: // meta con cota`); las secciones 3, 5 y 8 deben usar la misma convención.
- [04-gramatica.md] Consecuencia de la holgura 0,7 que conviene que la sección 9 o la 17 digan: la franja de valle (4,3; 5,7) km no se dibuja nunca, y San Fermo (Lombardía por Como) está a 5,5 km reales; el ejemplo de §4.7 lo pone a 4,0. Igual con (19,3; 20,7).
- [04-gramatica.md] En el ejemplo de Lombardía (§4.7) la sección solo dice que `stageKindOf` con un puerto de 13 km devuelve `reina` por `PASS_MIN_KM` y deja a la sección 5 declarar `Skeleton.kind` y `label` de `ud_montana` (V6); hay que comprobar que la sección 5 no lo declara `media`.
- [05-esqueletos.md] Recuento 31 frente a 32: la unión cerrada `SkeletonId` de §B.2 tiene 15 `ud_*`/`nc_*` y 16 `et_*` (31), pero el esqueleto, el índice y arquitectura §3.3 dicen «32 (15 + 17)». He escrito los 31 de la unión sin inventar el que falta; la pasada de coherencia decide si se añade un 32.º (y cuál) o si se corrige la cifra en cabecera, §B.1, decisión 1 y sección 3.
- [05-esqueletos.md] Tres `kind` se apartan de arquitectura §3.3 por la regla de `stageKindOf` (l. 72-97), que V6 hace ley: `ud_esprint_capi` es `media / Hills` (Poggio y Cipressa pasan de `WALL_MAX_KM` 3, banco §9.4.1), `ud_circuito` es `clasica / Classic` (todas sus cotas ≤ 2,5 km y no muere arriba), y `ud_muros_adoquin` y `ud_sterrato` llevan etiqueta `Cobbles` (cualquier `paves` la fuerza, l. 75). Las secciones 3, 9 y 13 (bandas por esqueleto) deben usar estos mismos kind/label.
- [05-esqueletos.md] En los esqueletos `llana` (`ud_esprint`, `et_llana`) la «cota testimonial» de arquitectura se escribe como `tendida` (tipada `llano`) porque cualquier segmento `puerto` excluye `Flat` (l. 77-78). V9 de la decisión 24 («sin cota ≥ 2,5 km a ≥ 5 % en los últimos 15 km») queda trivialmente cierto; la sección 9 debería decirlo o reescribir V9 sobre `tendida`.
- [05-esqueletos.md] `nc_ruta` es la única excepción a «un `kind` por esqueleto»: en zonas sin `cota` ≥ 3,3 km se instancia como `clasica / Classic` con `NC_RUTA_CLASICA` mediante `skeletonFor(id, geo)`. Añade una función y una constante exportadas a `skeletons.ts` que no están en §B.1, y V6 (sección 9) debe comparar contra `skeletonFor(...).kind`, no contra `SKELETONS[id].kind`.
- [05-esqueletos.md] Nombres nuevos en `skeletons.ts` no previstos en §B.1: `CANONICO` (o `Skeleton.canonico` rellenado desde él), `SESGO_TERRENO`, `ESCALON_TERRENO`, `candidatos()`, `skeletonFor()`, `NC_RUTA_CLASICA`, `SKELETON_IDS`. Conviene que §B.1 y la sección 3 los recojan.
- [05-esqueletos.md] Convención del motivo `meta`: he fijado `meta.km = cotaFinal.km + valle` en `cima_cerca`/`descenso_meta`/`valle`, `= aproxKm 2 + cotaFinal.km` en `muro_meta` (aproximación antes, cota en la línea) y `= cotaFinal.km` en `alto_*`. Las secciones 4 y 8 (rendido) deben usar exactamente esta convención.
- [05-esqueletos.md] Regla de subida máxima para `kind: 'media'`: para que `QUEEN_MIN_CLIMB_METRES` 3.200 no convierta una media en reina, he escrito que la persecución del desnivel (sección 8) escala hacia abajo si Σ km·g·10 de las dificultades supera 2.900 m en esqueletos `media`. Es una regla que la sección 8 tiene que implementar (no hay constante `ARCH` para ella; puede añadirse como `ARCH.veto.margenClaseMetros` 300 en la sección 12).
- [05-esqueletos.md] `ud_montana_alto`: para cumplir a la vez «peso 0,02 en .1» (§B.3) y que la rareza sea ≈ 2 % de los sorteos `mountain` en .1, le he dado `pesoBase` 60 (60 × 0,02 = 1,2 frente a 48 + 12 + 5). Si la coherencia prefiere `pesoBase` 1 y un multiplicador de clase distinto, hay que cambiar §B.3 y la decisión 37.
- [05-esqueletos.md] `geo.pesos`: además de los dos ejemplos de arquitectura §5.2 (`flandes`, `andes`) he decidido `italia_centro` {`ud_sterrato`: 2} y `francia_norte` {`ud_adoquin`: 2}. La tabla `ZONAS` de la sección 6 debe llevar esos cuatro valores en su columna `pesos`.
- [05-esqueletos.md] `ARCH.km.porClase` en un día: he escrito la regla de recorte `clamp(kmClase, sk.km[0], min(sk.km[1], maxPorClase))` con el extremo del esqueleto mandando si el rango queda vacío (una `ud_montana` en .2 mide 200 y no 180). La sección 7 (`kmDe`) y la 12 deben decir lo mismo.
- [05-esqueletos.md] La única fila `itt` de un día de tabla (mapa 02 §1) se asigna a `nc_crono`; si la coherencia prefiere `et_crono`, cambiar la fila `itt` de `SESGO_TERRENO` en §5.6.
- [05-esqueletos.md] Las 4 etapas `cobbles` de edición reciben `ud_adoquin_ligero` (no existe `et_adoquin`), única excepción a «etapas de edición reciben esqueletos de etapa» (I-9). Sección 11 y apéndice A deben anotarlo así.
- [05-esqueletos.md] Alternativas de nivel 2: he fijado que `[canonico, ...alternativas][season % (1 + alternativas.length)]` es la instancia de partida y que toda alternativa conserva `kind`, `label`, `meta` y `finalKind`. La sección 10 debe usar la misma fórmula (decisión 22 dice «season % n» sin decir si `n` incluye el canónico).
- [06-geografia.md] `TERRITORIOS` tiene 64 filas explícitas (los 56 países con equipos más 8 voluntarias: AR, CL, NZ, IE, SE, FI, LV, QA) y 69 países en `FALLBACK`; el esqueleto §C.1 decisión 13 y §D dicen «56» y «77 restantes». Se ha hecho así porque §D pide corregir el bug AR/CL con `cono_sur`, lo que exige filas para AR y CL. La pasada de coherencia debe unificar la cifra (64/69 en este texto) en las secciones 3, 15, 17 y 18 o decidir que solo haya 56 filas y AR/CL caigan a fallback (entonces `cono_sur` queda sin país).
- [06-geografia.md] Reina sin cordillera: para honrar «puerto de 20-30 km como finalesAlto: 'largo' por vuelta» en `cono_sur` se ha definido que `et_reina_blanda.requiere` no exige `relieve` y que la composición (sección 7) la ofrece como máximo UNA vez por vuelta cuando `Territorio.cordillera === null` y una zona de la ruta tiene `finalesAlto: 'largo'`. Es una excepción a la decisión 18 («reina solo en cordillera o zona montana/alta») que la sección 7 tiene que implementar y sellar (test «AR y CL: ≤ 1 reina por vuelta, siempre `alto`»).
- [06-geografia.md] `golfo` se ha escrito con `puerto: null` y `finalesAlto: 'corto'` (Jebel Hafeet como `alto_corto` de 7 km; Jebel Jais no existe) para respetar la decisión 13 («0 reinas en AE») y D8; contradice la fila de `arquitectura.md` §5.1 (jebel largo, 1 por vuelta). La sección 17 debe anotar el sacrificio y la 18 (D8) decir que aceptar D8 implica esto.
- [06-geografia.md] `race-fleche` no existe como id en `raceRoutes.ts`: la Flecha Valona es `race-huy` (Charleroi → Huy). El esqueleto §D cita «race-fleche → ardenas»; las secciones 15 y 19 deben usar `race-huy`.
- [06-geografia.md] `COUNTRIES` tiene 133 entradas (grep `^  { code:` en `packages/shared/src/countries.ts`), no 136 como dice `datos.md` §5.2; el texto usa 133 y 532 = 133 × 4.
- [06-geografia.md] El esqueleto dice que la tabla `ZONAS` tiene «29 filas»; `GeoZone` de §B.2 tiene 30 miembros (29 zonas con nombre más `generico`) y `Record<GeoZone, GeoSignature>` exige 30 filas. El texto dice «29 zonas, 30 filas»; conviene que la cabecera y la sección 3 lo digan igual.
- [06-geografia.md] El test (b) de `regions.test.ts` usa `race.championshipCountry` y `race.country` de `CalendarRace` (existen hoy: `calendar.test.ts` l. 10 y mapa 02 §1); el test (d) usa la lista literal de los 20 ids `cobbles` obtenida con grep sobre `calendar.ts` (`race-leon` es Tro Bro Léon). Si el redactor de la sección 15 (paso 2) prefiere exportar la lista desde `calendar.ts`, hay que cambiar el test aquí.
- [06-geografia.md] Se ha decidido que `RaceRow` NO gana `geo?: GeoZone` (contra arquitectura §5.3 e ingeniero §5.2): `RACE_REGION` es la única tabla, por id, como `RACE_COUNTRY`. La sección 3 (modelo, `RaceRow` l. 381-398) y la 11 deben decir lo mismo.
- [07-vueltas.md] `Itinerario` gana un campo `notas: string[]` (reparaciones anotadas: «reina → media_alto, ventana sin cordillera») que §B.2 no tiene; la pasada de coherencia debe añadirlo al glosario (sección 3) o decidir dónde se anota la degradación de la reina que la decisión 18 exige anotar.
- [07-vueltas.md] `kmDe` amplía el papel a `StageRole | 'un_dia' | 'un_dia_u23'` para dar a los nacionales sub-23 la fila `sub-23 [140, 40]` de `ARCH.km.porClase`; la firma de §B.2 solo tiene `StageRole | 'un_dia'`. Alternativa si no se quiere ampliar: que `nationalChampionships` lea `ARCH.km.porClase.NC.u23` directamente.
- [07-vueltas.md] `RouteContext` y `DEFAULT_ROUTE_CONTEXT` se definen en `tour.ts` con la forma { raceId?, country: string | null, raceClass, format, season } (country null → territorio fallback `generico`); §B.1 solo los cita en la firma de `stageMix`. Las secciones 3 y 8 deben usar la misma forma. Además `StageSpec` (no solo `CalendarStage`) gana `routeSource` y `arch` para que `stagesFrom` los conserve.
- [07-vueltas.md] `ARCH.itinerario.cronoescalada` = 0,08 es una clave nueva del bloque `itinerario` (§B.3 solo lista `avance` y `transicion`); el 0,25 del prólogo vive en `TOUR_SKELETONS[id].primera.prologo`. La sección 12 debe recogerlos (vienen de la decisión 42 / D3).
- [07-vueltas.md] Re-sellado inevitable: `calendar.test.ts` l. 211-221 («la primera etapa es siempre llana y nunca es la crono») pasa a admitir prólogo, porque la decisión 42 pone `prologo` en `roles[0]` con p 0,25 en n ≥ 6. §D.15 paso 7 dice «garantías de calendar.test.ts l. 184-246 sin tocar»: hay que ajustar esa frase del plan (las otras aserciones sí quedan intactas).
- [07-vueltas.md] §D.7 dice «vu_gran_vuelta para 9-21»; he decidido `vu_larga` [9; 14] y `vu_gran_vuelta` [15; 21] siguiendo arquitectura §7.2 y la lista `TourSkeletonId` de §B.2 (las cinco vueltas generadas de 9 a 11 etapas van por `vu_larga`). Confirmar.
- [07-vueltas.md] El itinerario consume el subflujo `arch|raceId` (según §B.2) y he asumido que la identidad de esqueleto por etapa en `generateStage` usa `arch|raceId|{i}` para no entrelazar tiradas; la lista cerrada de subflujos de `edition.ts` (§B.2) y la sección 8 deben nombrarlo así.
- [07-vueltas.md] Contrato con las secciones 5, 6 y 8: en composición solo la reina se degrada por geografía (cordillera / montana / alta); los demás papeles son demandas que `generateStage` rinde con el mejor esqueleto que la zona admite. Para que `race-sharjah` conserve crono y final en alto (`calendar.test.ts` l. 248-255), `ZONAS.golfo` debe tener `puerto` no nulo (Jebel Hafeet 10,8 km, Jebel Jais 20 km, mapa 07 §2.2) aunque `TERRITORIOS.AE.cordillera` sea null.
- [07-vueltas.md] He conservado las claves `flat | hilly | mountain` como índice de las cuatro tablas de `ROUTE` que se conservan (`itt*`, `lastDecisiveChance`, `lastSummitShare`, `selectiveMinFraction`), calculadas con `mixTerrain(row.terrain)`; §A punto 1 dice que «los tres terrenos de MixTerrain desaparecen». Desaparecen como modelo de composición, no como índice: conviene que la cabecera lo diga así.
- [07-vueltas.md] En `vu_gran_vuelta` la última etapa se decide con `ultima = { llana 0,85; cri 0,15 }` en vez del paso 2 de `mixRoles`, así que `ROUTE.grandTourLastDecisiveFactor` 0,4 queda conservado como constante pero sin uso en lo generado; §B.1 lo lista entre lo que se conserva y la sección 12 debe decir que ya no lo lee `composeTour`.
- [08-algoritmo.md] Las dos constantes de bajada del esqueleto §B.3 se solapan: `ARCH.motivo.descenso.kmPorDesnivel` (clamp(len·g·10/55, 2, 10)) y `ARCH.colocacion.bajadaTrasPuerto` ([0,6; 0,9] × km del puerto). En 8.6 he decidido que la primera da la LONGITUD de la bajada y la segunda la FRACCIÓN de desnivel que se devuelve (fija la pendiente, acotada a [−8; −3]); la intención de `bajadaTrasPuerto` en §B.3 está vacía y la sección 12 debe escribirla así o corregir 8.6.
- [08-algoritmo.md] He usado `ARCH.motivo.cadena.separacion` [1; 6] km (los «enlaces de 1-6 km» de arquitectura §3.1 para `cadena`) que NO está en la tabla §B.3; la sección 12 debe añadirla o la sección 4 debe decir de dónde sale la separación de una cadena.
- [08-algoritmo.md] Clave de etapa en los subflujos de identidad: §B.2 solo escribe `arch|raceId` y `firma|raceId`. Para una etapa de vuelta generada he usado `arch|raceId|stageIndex` y `firma|raceId|stageIndex` (y `arch|raceId` a secas queda para la composición de la sección 7); para edición real `arch|raceId|e{i}|{editionKey}`. La sección 7 y la 10 deben usar la misma convención.
- [08-algoritmo.md] El token `slot` del subflujo `dib` lo he extendido a `e{k}` para el k-ésimo enlace (los enlaces no son huecos del esqueleto y necesitan semilla propia) y a `|hijo{h}` dentro de cadena/racimo/circuito; §B.2 solo dice `dib|raceId|i|season|slot|i{intento}`. La sección 10 (edition.ts) debe recoger esa forma o proponer otra.
- [08-algoritmo.md] `render.ts` no tiene fichero de test en la lista de §B.1 (`grammar/{geo,motifs,skeletons,place,veto,edition,tour,generate,geometry}.test.ts`): he puesto los tests de `renderSkeleton`, `normalizeEnlaces`, `garantizaClase` y `emitirPancartas` dentro de `generate.test.ts`. Si el plan (sección 15) prefiere `grammar/render.test.ts`, hay que añadirlo a §B.1.
- [08-algoritmo.md] He añadido dos medidas a `routeCensus` que no están en `RouteStats` de §B.2: una columna `garantias` (cuántas etapas pasan por las reglas 1 a 4 de `garantizaClase`, objetivo < 2 % del calendario) y `breakAppealEstimado` calculado sobre segmentos sin `sampleProfile` (este sí está en §B.2). La sección 13 debe decidir si `garantias` entra en `RouteStats` o se elimina de 8.9.
- [08-algoritmo.md] `fraseDe` es una función interna de `generate.ts` que no aparece en §B.1; los tres ejemplos literales de frase de 8.13 son juicio mío y la sección 10/11 (ficha) debe usar los mismos o cambiarlos en los dos sitios.
- [08-algoritmo.md] En 8.4 he decidido que con `ARCH.edicion.activa` false o `nivel` 0 el objetivo de desnivel (`dPlusObjetivo`) SÍ se sortea (es de la etapa, no de la edición) y que en una etapa de edición real los pasos 2 a 4 de `ed` no tiran (los motivos no cambian entre temporadas, solo el dibujo). La sección 10 debe confirmar que eso es lo que `edition.test.ts` sella.
- [08-algoritmo.md] Orden de comprobación de `verify` (V10, V15, V6, V7, V1 a V4, V5, V8, V9) lo he fijado por coste creciente; la sección 9 debe usar el mismo orden o cambiar 8.11.
- [09-vetos.md] `climbSize` (stageKind.ts l. 36-42) y `climbMetres` (l. 27-33) NO están exportadas hoy; la sección decide que el paso 5 les añade `export` sin tocar el cuerpo. La fila de `stageKind.ts` en §B.1 del esqueleto solo menciona `SUMMIT_RUN_IN_KM`: hay que añadir esa exportación a la fila y al paso 5 del plan (sección 15).
- [09-vetos.md] Constantes nuevas bajo `ARCH.veto` que no están en §B.3 y que la sección 12 debe incorporar: `segmentoMinKm` 0,5, `kmTolerancia` 0,05, `pendientes` {gMax 20, gMin −14, subidaGMin 1}, `puertoLargoKm` 15, `puertoDplusMax` {mar 500, colina 800, media 1.300, alta 2.100, altiplano 1.500}, `llana` {dPlusMax 1.800, cotaKm 2,5, cotaG 5, ventanaKm 15}, `calendario` {muroMin 0,01, puncheurMin 0,08}. Los valores de `puertoDplusMax` son decisión de esta sección apoyada en mapa 07 §4.2-4.3; la decisión 24 solo dice «D+ de un solo puerto integrado por tramos» sin cifras.
- [09-vetos.md] `geometry.ts` gana dos funciones no listadas en §B.1: `climbKmOutsideLast30(profile, kmToGo = ARCH.reina.subidaLejanaKm)` y `subidaLejanaShare(profile)`. Añadir a la fila de geometry.ts y a la sección 3 si reproduce el bloque.
- [09-vetos.md] V8b se aplica a TODO esqueleto `kind: 'reina'` (decisión 6 dice «toda reina, blanda incluida»), y eso incluye `ud_montana_alto`: con `puerto`×0 sería literalmente reina-150 con 100 km de enlace. Esta sección fija su hueco `puerto` en `n: [1; 1]`; el borrador de la sección 5 (l. 33) lo tiene como `[0; 1]` y hay que cuadrarlo (o bien la pasada de coherencia decide eximir a `ud_montana_alto` de V8b, en cuyo caso hay que cambiar la decisión 6).
- [09-vetos.md] Consecuencia de V5(b) sobre `repecho` en un día: la meta `repecho` de un esqueleto `ud_*` se instancia en [1; 2,2] km (intersección de `ARCH.meta.repecho.km` [1; 2,9] con el techo de muro 2,2); en etapa sigue en [1; 2,9]. La tabla de MetaKind de la sección 4 (l. 82 del borrador) debe recogerlo.
- [09-vetos.md] V5(c), la ventana [3; 17] km a meta de la decisión 5, se aplica solo a `ud_montana` (no a `ud_circuito`, `ud_muros` ni a los muros de meta, cuya última cota corona a 0-3 km en lo real). Para `ud_montana` el valle de `cima_cerca` queda intersecado a [3; 4,3] y el de `descenso_meta` a [5,7; 17]. Comprobar que las secciones 4, 5 y 8 usan la misma intersección.
- [09-vetos.md] Dos denominadores para «subida fuera de los últimos 30 km»: V8b usa fracción del km de SUBIDA (≥ 0,25, como banco/I-18); la columna de balance v43 §7 (6-38 % reales, canónica 0 %) y la banda de calendario de la sección 13 («ninguna en 0 %, p10 ≥ 5 %») usan fracción del km TOTAL de la etapa. `routeCensus` imprime las dos. La sección 13 debe decir cuál lee su banda (esta sección asume la de v43, km total).
- [09-vetos.md] V7 se define con `finalKindDe(meta)` (mapa MetaKind → FinalKind) porque `ud_montana` y `et_reina_valle` no tienen un único `Skeleton.finalKind` (arquitectura §6.4); `skeletons.test.ts` sella que, cuando `sk.finalKind` está declarado, coincide con `finalKindDe(sk.meta)`. Cuadrar con la sección 5 (columna finalKind del catálogo) y con el test de identidad de la sección 10.
- [09-vetos.md] Orden de comprobación de `verify` tomado del borrador de la sección 8 (l. 180): V10, V15, V6, V7, V1 a V4, V5, V8, V9. Si la sección 8 cambia, cambiar aquí (§9.1 y §9.3 paso 6).
- [09-vetos.md] Conjuntos que promete cada MetaKind a V16 tomados del borrador de la sección 4 (`muro_meta` ≤ 1,0 → {muro}; > 1,0 → {puncheur, alto}; `cima_cerca` → {descenso, puncheur}; `descenso_meta` → {descenso, sprint_reducido}). Si la sección 4 los cambia, cambiar la fila V16.
- [09-vetos.md] La calibración de V12 sobre pares reales se define aquí como: todos los pares de las 177 reales con mismo `stageKindOf().kind`, mismo `finalKindOf()` y km ± 10 %, tope = p90; los tres pares nombrados por datos (Ronde/E3, Amstel/Brabant, Lombardía/Lieja) solo como comprobación impresa si existen en `STAGE_FEATURES`. No se ha comprobado en `stageFeatures.ts` cuáles de esas seis carreras existen; la sección 13 o el paso 9 del plan deben asumir el mismo procedimiento.
- [10-identidad.md] Ruta de la migración: el esqueleto §B.1 la sitúa en `packages/db/migrations/00NN_race_routes_kind.sql`, pero esa carpeta no existe en el repositorio; las migraciones viven en `packages/db/drizzle/` (última: `0039_el_recorrido_es_del_mundo.sql`). La sección escribe la ruta real y lo dice; la pasada de coherencia debe unificar con §B.1, la sección 15 (paso 10) y la sección 11.
- [10-identidad.md] Semántica de `ARCH.edicion.nivel`: §B.3 fija el valor 1 y D7 dice «nivel 1; nivel 2 donde el esqueleto declare alternativas». Lo he resuelto con `nivelEfectivo = nivel === 0 ? 0 : (sk.alternativas ? 2 : nivel)` (la constante a 2 es no-op para esqueletos sin alternativas). Confirmar que las secciones 12 y 18 escriben lo mismo.
- [10-identidad.md] `ARCH.edicion.motivoNuevo` 0,35: arquitectura §4.3 lo definía como «un hueco opcional que estaba a 0 pasa a 1, o al revés» respecto de una base sin dados; como la temporada 0 ahora tira dados (decisión 21) no hay base, y lo he fijado como p de que un hueco opcional esté AUSENTE en una edición (presente con 0,65). Confirmar con la sección 8 (instanciación) y la 12.
- [10-identidad.md] Circuitos: he decidido que en esqueletos con `circuito` de firma NO se aplica `kmJitter` (el km total es aproximación + vueltas × kmVuelta y el jitter es el de vueltas, `vueltasJitter` 0,5). Confirmar con la sección 8 (`normalizeEnlaces`, circuitos) y la 4.
- [10-identidad.md] `finalKind` fijo entre ediciones: lo deduzco de V7 (`finalKindOf(profile) === Skeleton.finalKind` por intento) y retiro la excepción de arquitectura §6.4 (`ud_montana` y `et_reina_valle` podían cambiar de cubeta). La sección 9 (V7) y la 5 (qué esqueletos declaran `finalKind`; `ud_montana` es de un día y quizá no lo declara, en cuyo caso el test compara `null` con `null`) deben ser coherentes con esto.
- [10-identidad.md] `sim/world.ts` l. 171-181 (`calendarioDe`, lee `kind` de `SEASON_CALENDAR` al cargar el módulo) no está en la lista de cuatro lectores de la decisión 23; he decidido que NO cambia porque `kind` es identidad y la bolsa es estadística por división. Confirmar en la sección 11 o 13.
- [10-identidad.md] `raceForSeason` lanza `Error('carrera desconocida')` con un id inexistente (como `packages/db/src/callups.ts` l. 266) en vez de devolver `undefined`; es decisión mía, sin fuente en las propuestas.
- [10-identidad.md] Banda «correlación entre carreras distintas del mismo esqueleto < 0,6» (ingeniero §6.3) convive con V12 (`ARCH.anticlon.maxCorrelacion` provisional 0,85, calibrado en el paso 9): he escrito que las dos aplican (V12 veto sobre cualquier par; 0,6 banda sobre pares del mismo esqueleto). Las secciones 9 y 13 deben decir lo mismo o la coherencia elegir una.
- [10-identidad.md] `SEASON_CALENDAR` se reexporta en `packages/engine/src/index.ts` l. 77 (medido hoy con grep); arquitectura §6.2 decía l. 83. He escrito 77.
- [10-identidad.md] El test `stagesForSeason(id, 0)` `toBe` `r.stages` (misma referencia) exige que `SEASON_CALENDAR` sea el propio array memoizado de la temporada 0 y no una copia; las secciones 8 y 14 (memoización, calendario perezoso si se supera el techo) deben conservarlo o la coherencia relaja el test a `toEqual`.
- [10-identidad.md] Los auxiliares del test (`firmaDe`, `kmDeStage`, `paresMismoEsqueleto`, `paresMismaTripleta`, `withArch`, `resetSeasonCache`) los define la propia sección como auxiliares de `edition.test.ts`; si otra sección (13 o 15) ya nombra un auxiliar equivalente para cambiar `ARCH` en tests, unificar el nombre.
- [11-real.md] Etiquetas nuevas de la decisión 38 (`Circuit`, `Wall finish`, `Prologue`, `Hill climb`, `Mountains classic`): §B.2 dice que `GeneratedStage.label = stageKindOf(...).label`, pero `stageKindOf` no puede deducir `Circuit` ni `Mountains classic` del perfil. En §11.5 regla 3 he decidido que `generateStage` pone `Skeleton.label` DESPUÉS de que V6 iguale el `kind`, y que `calendarStageSpec` no recalcula la etiqueta de etapas con `routeSource !== 'real'` (devuelve `stage.label`, congelada en `race_routes.label`). Las secciones 3, 5 y 8 deben escribir el mismo mecanismo o la coherencia debe corregir §11.5.
- [11-real.md] Huella de las tres grandes vueltas: la e21 de `race-france` es `edicion` (mapa 06 §1) y su perfil cambia en el paso 8 al pasar por `et_llana`, así que no puede sellarse entera. He decidido sellar ESTRUCTURA de las 21 etapas (kind, timeTrial, km, restAfter) y perfil solo de las reales (`huella: null` en la e21). La sección 15 (paso 1) y la decisión 28 dicen «3 grandes vueltas enteras»; hay que alinear el enunciado.
- [11-real.md] Nombre del tipo agregado por carrera: §B.2 solo da los tres valores ('real' | 'mixto' | 'generado') sin nombre. He usado `RaceRouteSource` y `raceRouteSourceOf(stages)` en `grammar/generate.ts`; la sección 3 debe usar el mismo nombre o el glosario debe fijarlo.
- [11-real.md] Nombre del fichero literal de huellas: he creado `routes/realFingerprint.sealed.ts` (literal pegado, no regenerable por el test) además del test `realFingerprint.test.ts` de §B.1; si la sección 15 prefiere las huellas dentro del propio test, hay que unificar.
- [11-real.md] Texto de carrera `mixto` en la web («Recorrido parcialmente real: N de M etapas») y el fichero `apps/web/src/pages/Race.tsx` como sitio de la marca: el esqueleto solo fija los tres textos por etapa; el texto de carrera y el fichero de la web son elección mía y deben confirmarse con la sección 10 y el paso 10 de la 15.
- [11-real.md] `sim/world.ts` l. 169-193 (`CALENDARIO` calculado al cargar desde `SEASON_CALENDAR`): he decidido que se queda como está (banco de población) y se mide su reparto de `kind` antes y después del paso 8; la sección 13 debe tener la fila correspondiente en la tabla de re-sellado.
- [11-real.md] La cifra re-sellada del 49 en etapas reales: he escrito `N_MEDIDO_EN_EL_PASO_8` como marcador y afirmado que solo un cambio de dato real puede moverla después; ninguna fuente da la cifra y no la he estimado. La sección 15 paso 8 debe repetir que se mide y se escribe con causa, no un objetivo numérico.
- [12-constantes.md] He añadido dos claves que §B.3 no lista: `ARCH.itinerario.prologoP` 0,25 y `ARCH.itinerario.cronoescaladaP` 0,08 (decisión 42 y D3 las exigen pero no tenían nombre canónico). La sección 7 (vueltas) y la 18 (D3) deben usar esos nombres; si el redactor de la 7 las puso en otro sitio, hay que unificar.
- [12-constantes.md] Los km de `et_prologo` [3; 8] y `et_cronoescalada` [8; 25] los he dejado en `Skeleton.km` (sección 5) y no en `ARCH.km`; `kmDe` para `prologo`/`cronoescalada` lee `Skeleton.km`, para `cri` lee `ROUTE.itt*`. La sección 7 tiene que decir lo mismo.
- [12-constantes.md] `ARCH.km.porClase` reduce los 12 `StageRole` a cinco columnas (`llana`, `media`, `reina`, `corta`, `unDia`) con el mapeo escrito en 12.7, y NC como `{ ruta, sub23 }` elegido por `championshipCategory` (`calendar.ts` l. 70). La sección 7 debe usar el mismo mapeo.
- [12-constantes.md] `ARCH.pesoPorClase`: he decidido tabla puramente numérica con ausente = 1, y la cláusula «`ud_adoquin` 0 en .2 fuera de `adoquin ≥ 2`» la resuelve `Skeleton.requiere` de `ud_adoquin` (adoquín ≥ 2) en la sección 5, no un 0 condicional. La sección 5 debe codificar ese `requiere` y poner `NC` = 0 en todo `ud_*`/`et_*` y `nc_*` = 0 fuera de NC.
- [12-constantes.md] `ARCH.pesosComposicion`: la tabla de arquitectura §7.3 marca `llana_viento` 0,25 «(viento ≥ 2)» en `llano`; he decidido que si `geo.viento < 2` ese peso se suma a `llana`. La sección 7 debe aplicar la misma regla.
- [12-constantes.md] El test de coherencia de `ARCH` lo he puesto en `grammar/motifs.test.ts` (bloque «ARCH es coherente con routes/ y STAGE») porque el esqueleto no lista un `constants.test.ts`; exige exportar `PASS_MIN_KM`, `WALL_MAX_KM` y `QUEEN_MIN_CLIMB_METRES` desde `stageKind.ts` (hoy son `const` sin export, l. 60-64). La sección 15 (paso 3) debe incluir esa exportación.
- [12-constantes.md] Línea de la cita a `stageKind.ts` para `SUMMIT_RUN_IN_KM`: no existe hoy (es nueva); lo he puesto en la tabla de «lo que NO cambia» como la única modificación, sin línea. Si la sección 11 le da línea o nombre distinto, alinear.
- [12-constantes.md] Longitud: 332 líneas contra 250 objetivo; el exceso son las tablas (12.12 literales que migran y 12.13 lo que no cambia). Si hay que recortar, 12.12 puede reducirse a las seis líneas que el esqueleto cita (247-248, 276-283, 358-363, 451-454, 477-478, 496).
- [13-banco.md] `test:rapido` excluye `packages/engine/src/sim/**` (`package.json` l. 20), así que `sim/routeCensus.test.ts` (listado en §B.1) NO corre en cada push. He resuelto que las bandas de `ROUTE_CENSUS_TARGETS` se afirman en `routes/grammar/calendario.test.ts` (sí en `test:rapido`) y que `sim/routeCensus.test.ts` solo prueba el censo sobre perfiles literales y corre con `test:bancos`. La sección 15 (plan) y la 14 (rendimiento) deben decir lo mismo, y la frase del esqueleto «`routeCensus` en `test:rapido`» debe leerse así.
- [13-banco.md] Para el pareado del paso 9 he decidido que el generador viejo vive en DOS ficheros temporales, `sim/legacy/profileGenLegacy.ts` (primitivas y los ocho `xxxSegments`) y `sim/legacy/calendarLegacy.ts` (`oneDaySpec`, `stageMix`, `mixRoles` y el cableado de `buildRace` de hoy, que producen `CalendarRace[]`), porque un solo fichero de builders no basta para construir el calendario viejo. §B.1 solo lista el primero; el plan (sección 15, paso 9) debe listar los dos y borrarlos juntos.
- [13-banco.md] He decidido que cada función de banco gana un último parámetro opcional `calendar: CalendarRace[] = SEASON_CALENDAR` (`analyzeCalendarQueens`, `findStage` de `realQueens.ts`, `smallTours.ts`, `timeTrials.ts`) y que la selección de las 8 más duras, hoy inline en `invariants.test.ts` l. 486-499, se extrae a `sim/saturation.ts::hardestOneDay(calendar, n = 8)`, más un script `pnpm sim:pareado` (`sim/pareado.ts`) y `sim/preRegistro.ts`. Ninguno de los cuatro está en §B.1; la pasada de coherencia debe añadirlos al glosario y al paso 9 del plan o sustituirlos por otro mecanismo de pareado.
- [13-banco.md] Reloj de `calendarQueens.test.ts`: con la muestra estratificada de ~30 etapas la aritmética de la regla ×4 da 3.716 s, por encima de los 3.600 s de hoy; he fijado `timeout: 4_000_000`. Si la sección 15 o el esqueleto citan 3.600 s para ese test tras el paso 9, hay que alinear (la decisión 34 del esqueleto dice «reloj 3.600 s con 4»).
- [13-banco.md] `FrozenQueen` (tipo nuevo en `sim/frozenSkeletons.ts`) y `GENERATED_QUEENS` no tienen forma en §B.2; he escrito la interfaz completa en §13.5 (incluye `huellaFNV` del perfil rendido, sellada con causa). Comprobar que la sección 8 exporta `describeProfile` con lo que §13.5 le pide (puertos con km, g y posición) y que la firma de `renderSkeleton` admite una instancia `motivos` ya colocada más `routeRng(seed)`.
- [13-banco.md] `breakAppealEstimado` se define reproduciendo la regla de `simulate.ts` l. 1698-1702 con `STAGE.breakAppealClimbWeight` y `STAGE.breakAppealUphillBonus` (4 y 0,35 según mapa 03 §4.1) y aproximando `isUphillFinish` por `finishType ∈ {alto, muro, puncheur}`. Si el dueño del motor prefiere exportar la función real desde `simulate.ts`, es un cambio de la sección 8/14, no de esta.
- [13-banco.md] `ROUTE_CENSUS_TARGETS` incluye la fila «p50 de reina de gran vuelta generada ≥ 3.000» con población vacía en E1 (las tres grandes vueltas son reales; las generadas son E12): se imprime «n insuficiente» hasta entonces. La sección 17 (lo que E1 no hace) debería citarlo para que nadie lo lea como banda muerta.
- [14-rendimiento.md] Cuántas temporadas construye `sim/routeCensus.test.ts`: la sección 10 (borrador/10-identidad.md l. 228) dice «temporadas 1 a 3 además de la 0» y la sección 13 (borrador/13-banco.md l. 104) dice «las temporadas 1 y 2». La sección 14 asume 0 + 3 (tope 2,5 + 3 s más cuatro censos de 0,57 s); la pasada de coherencia tiene que fijar una cifra en las tres secciones.
- [14-rendimiento.md] Coste que la sección 10 atribuye a `edition.test.ts`: borrador/10-identidad.md l. 150 dice «seis temporadas memoizadas, ≤ 6 s por el techo de ARCH.arranque»; con `techoMs` 2.500 para la temporada 0 y `porTemporadaMs` 1.000 para cada una de las cinco adicionales, el tope es 7,5 s (así lo pone la tabla de §14.5). Unificar la cifra.
- [14-rendimiento.md] La sección 14 fija un tope de 8 temporadas en memoria (`MAX_TEMPORADAS_EN_MEMORIA = 8`, literal de `edition.ts`, no de `ARCH`, con expulsión de la más antigua) y la reutilización por referencia de las 177 etapas `real` entre temporadas (`Map` por `raceId` en `buildRace`); la sección 10 describe la memoización por `Map` sin tope ni reutilización. No se contradicen, pero la sección 10 debería citarlos o remitir a la 14.
- [14-rendimiento.md] El modo perezoso de §14.6 pone `profile`, `arch` y `label` como propiedades de acceso (`Object.defineProperty` con `get`) sobre `CalendarStage`; la sección 3 (§3.11) declara `arch?` y `label` como campos planos. Si el paso 8 activa el modo perezoso, el tipo no cambia (un getter cumple la interfaz) pero la sección 3 o la 15 deberían decirlo en una línea.
- [14-rendimiento.md] La tabla de §14.1 cita para `sim/world.ts` las líneas 169-186 (`calendarioDe`) y 188-192 (`CALENDARIO`) comprobadas contra el fuente de hoy; `juicios/motor.md` §5 riesgo 1 cita «l. 171». Ambas son compatibles, se deja constancia por si la coherencia prefiere una sola forma de citar.
- [15-plan.md] Directorio de migraciones: §B.1 dice `packages/db/migrations/00NN_race_routes_kind.sql`, pero en el repositorio las migraciones de drizzle viven en `packages/db/drizzle/` (la última es `0039_el_recorrido_es_del_mundo.sql`, así que NN = 40). En el paso 10 cito el nombre de §B.1 y el directorio real; la pasada de coherencia debe corregir §B.1 o confirmar el nombre.
- [15-plan.md] `test:rapido` excluye `packages/engine/src/sim/**` (`package.json` l. 20), así que `sim/routeCensus.test.ts` de §B.1 NO puede correr en cada push. He resuelto: `sim/routeCensus.test.ts` son tests de unidad del censo (corren con `test:bancos`) y las bandas `ROUTE_CENSUS_TARGETS` sobre el calendario viven en `routes/grammar/calendario.test.ts`, que sí está en `test:rapido`, creado en el paso 0 con `it.todo` y encendido entero en el paso 8. Las secciones 13 y 14 deben decir lo mismo (decisión 31 habla de «routeCensus en test:rapido»).
- [15-plan.md] §D pide en el paso 1 «legacy.ts con los ocho builders y tabla pareada 'igual dentro del ruido'», pero en el paso 1 no existe `renderSkeleton` (render.ts es del paso 4). He partido la prueba: en el paso 1 la pareada es a nivel de motivos (mismo número y orden de dificultades que cada `xxxSegments`, 300 de 300) y en el paso 4, ya con `renderSkeleton`, la pareada por `routeCensus` (|Δ p50| de dPlus, longestClimbKm y kmAfterLastClimb menor que la σ entre semillas). Alternativa si la coherencia lo prefiere: adelantar `render.ts` al paso 1. Además, el paso 1 crea `motifs.ts` y `skeletons.ts` SOLO con los tipos de §B.2 para que `legacy.ts` compile.
- [15-plan.md] `golden.test.ts` «se borra en el paso 8» (§B.1), pero la tabla pareada del paso 9 necesita un calendario viejo demostrado. He decidido que en el paso 8 las 1.418 huellas se mueven a `sim/legacy/golden.test.ts`, que exige que `legacyCalendar()` las reproduzca, y se borran con `sim/legacy/` al cerrar el paso 9. Por lo mismo, `sim/legacy/profileGenLegacy.ts` contiene, además de los ocho generadores, copias de `oneDaySpec`, el `stageMix` viejo (`mixRoles`, `mixKm`), `MixTerrain` y la rama vieja de `buildRace` como `legacyCalendar()`; §B.1 solo dice «copia del generador viejo».
- [15-plan.md] La aserción `stagesForSeason(id, 0) === SEASON_CALENDAR` que §D sitúa en el paso 6 solo puede ser verdad desde el paso 8 (hasta entonces `SEASON_CALENDAR` es el calendario viejo). En el paso 6 va como `it.todo` y se enciende en el paso 8; el test de identidad del paso 6 usa `raceForSeason`, que construye por la ruta nueva sin depender de `SEASON_CALENDAR`.
- [15-plan.md] La delegación de `stageMix` en `composeTour` la he puesto en el paso 8 y no en el 7, porque cualquier tirada añadida a `mixRoles` (prólogo) desplaza las 72 vueltas y eso es cambio de conducta; en el paso 7 `composeTour` se escribe y se prueba sin llamador. Sección 7 debe decir lo mismo.
- [15-plan.md] `arquitectura.md` §12 cita `targets.ts` l. 79-84 para la «mediana de 2.023»; verificado en el repositorio, la frase está en la l. 88. Cito l. 88.
- [15-plan.md] Uso `grammar/regions.test.ts` para `RACE_REGION` (§D paso 2 lo nombra) aunque la lista de tests de §B.1 no lo incluye; añadirlo a §B.1.
- [15-plan.md] §A punto 10 y la cabecera de §D 15 dicen «once pasos», pero el plan enumera doce (del 0 al 11). He escrito «doce pasos, del 0 al 11»; la cabecera debe alinearse.
- [15-plan.md] El coste total (22 sesiones de código más 1 de contenido, 4 h de máquina con techo de 8 h) es estimación de este documento y así lo digo; no hay fuente medida para las sesiones salvo la comparación con las 12 a 15 de `ingeniero.md` §12.
- [16-galeria.md] La sección añade un campo a `GeneratedStage.arch` que no está en §B.2: `rechazos: Veto[]` (uno por intento fallido, lo que `verify` devolvió). Coste cero porque `verify` ya lo produce; la sección 3 (modelo) tiene que incorporarlo al tipo o, si la pasada de coherencia lo rechaza, la columna «Intentos y rechazos» de 16.4 se queda solo con `intentos` y `degradado`.
- [16-galeria.md] La comprobación de disponibilidad esqueleto × zona se cita como `admite(geo: GeoSignature, sk: Skeleton): boolean` exportada por `grammar/geo.ts` (la decisión 12 nombra `admite()` y `degradar()` pero §B.1 no la lista entre los exports de geo.ts ni fija su firma). La sección 5 o la 6 tienen que fijar el nombre y la firma; la galería usa la que se decida.
- [16-galeria.md] El `StageRole` y el `terrain` de cada esqueleto `et_*` se toman «de su fila en la tabla de la sección 5»; esa tabla debe traer explícitamente las dos columnas (papel y terreno) para que el script las lea sin inventar un mapa propio.
- [16-galeria.md] Se decide que `docs/galeria-recorridos/` va a `.gitignore` (unos 12 MB de techo, medido por extrapolación: 2,2 KB por SVG sobre 1.418 etapas de hoy) y que lo versionado es `docs/galeria-revision.json` con las respuestas del dueño, resumidas en la nota v61 de `balance.md` en un apartado del paso 4. La sección 15 (plan, paso 4) y la cabecera de la nota v61 deben nombrar ese fichero y ese apartado con el mismo nombre.
- [16-galeria.md] En el paso 4 `verify` no está completo (llega en el paso 5); la sección dice que la galería del paso 4 corre con los vetos que `skeletons.test.ts` ya usa (V1 a V4, V6, V7) y lo imprime en el índice. La sección 15 debe confirmar que esos son exactamente los vetos disponibles en el paso 4.
- [16-galeria.md] Los números medidos al redactar (51 ms y 3,1 MB para 1.418 SVG, 2,2 KB de media, extremos 0,4 y 10,9 KB) salen de un script de diez líneas sobre el `dist` de hoy, no de ningún mapa; si la pasada de coherencia prefiere solo cifras de mapas y juicios, se pueden quitar dejando el 0,40 ms por etapa del juez motor §1.
- [17-riesgos.md] Cubeta < 1.500 m de reinas: `et_reina_blanda` tiene D+ total en [1.500; 2.500] (decisión 8; la sección 5 la mide en 2.138 m), pero la banda del censo de la sección 13 (l. 117) exige «< 1.500 ≥ 5 % del total» y `calendarQueens.test.ts` exige el estrato `<1500` poblado. Con ese suelo nadie puebla < 1.500 por diseño; la pasada de coherencia tiene que decidir si el suelo de la blanda baja (por ejemplo a 1.200, como `queenLowDplusRange` hoy) o si la banda del censo se escribe sobre `dPlusBloques` en vez de `dPlus`. El riesgo 10 está escrito remitiendo a D6 y al mensaje de fallo de la sección 13, sin resolver esto.
- [17-riesgos.md] Test de ejemplo del riesgo 5 (300 de 300 `muro_meta` → `finishType 'muro'`): lo he escrito con `renderMotif` del motivo `meta` solo; si el rendido de la aproximación (`aproxKm` 2, `aproxAmp` 2,5) vive en `renderSkeleton` y no en `renderMotif`, el fixture debe ser `generateStage` con `fixed.skeleton: 'ud_muro_final'`. Las secciones 4 y 15 definen ese test del paso 3 y deben mandar sobre mi versión.
- [17-riesgos.md] Textos de ficha para `altitud`: la sección 11 fija «llano abierto» para `viento ≥ 2` pero no encontré un texto canónico para `altitud` en `alta`/`altiplano`; en el riesgo 1 lo describo como «describe los puertos, no el aire» y remito la tabla a la sección 11. Si la sección 11 no la tiene, hay que escribirla allí.
- [17-riesgos.md] Riesgo 4: las cifras 7,9 % de `kmSubidaShare` y `breakAppeal` 0,31 para un circuito de 14 km × 9 con muro de 1,1 km son aritmética sobre la fórmula citada de mapa 03 §4.1 (`clamp(4·kmSubida/total + 0,35·[alto], 0, 1)`), no medida; así se dice en el texto, pero el paso 0 debería confirmarlas con `routeCensus`.
- [17-riesgos.md] Longitud: 135 líneas sin envolver (una línea por párrafo, como el resto del borrador) frente a las 180 objetivo; el contenido obligatorio de §D está entero (14 riesgos, lo que E1 no hace, tabla resumen, dos tests de ejemplo y la lista de deudas de la nota v61). Si hace falta más, lo natural es ampliar 17.2 con una fila por decisión del dueño.
- [18-dueno.md] D3, nombre de las constantes: el borrador de la sección 7 pone la p del prólogo en `TOUR_SKELETONS[*].primera.prologo` (0,25; 0,3 en `vu_gran_vuelta`) y la de la cronoescalada en `ARCH.itinerario.cronoescalada`; el borrador de la sección 12 declara `ARCH.itinerario.prologoP` 0,25 y `ARCH.itinerario.cronoescaladaP` 0,08. La sección 18 usa `primera.prologo` y `ARCH.itinerario.cronoescaladaP`; hay que unificar en 7, 12 y 18 (y en §B.3 si se añade).
- [18-dueno.md] D3, apagado: la sección 18 decide que con p 0 la tirada de `rand()` se consume igual, para que apagar prólogo o cronoescalada después no vuelva a desplazar las composiciones de las 72 vueltas. Las secciones 7 y 8 deben adoptar esa regla en `composeTour` o la sección 18 debe quitarla.
- [18-dueno.md] D9, techo WT: §B.3 y las secciones 7 y 12 codifican WT 260 mientras el mapa 07 §4.1 da «techo UCI 280 salvo excepciones como Sanremo». La sección 18 justifica el 260 por `ARCH.km.porClase` un día WT [200, 60] y por las 36 filas con km explícito; confirmar que 7 y 12 dan la misma razón.
- [18-dueno.md] D7, variante «casi nunca»: la sección 18 fija la mitad (`kmJitter` 0,03, `vueltasJitter` 0,25, `motivoNuevo` 0,15) e interpreta `vueltasJitter` 0,5 como probabilidad de ±1 vuelta; confirmar con las secciones 10 y 12 el significado de `vueltasJitter` y si la variante se quiere documentar.
- [18-dueno.md] D5, si el dueño dice «sí» sin cifra: la sección 18 propone 0,05 en .2 y 0 en el resto, y afirma que los puntos los da la clase de la fila (`uci.ts`, mapa 02 §2), por lo que un critérium «no puntuable» es un formato nuevo (E12). Verificar la afirmación sobre `uci.ts` contra el mapa 02 §2 antes de cerrar.
- [18-dueno.md] D2, nombres de los tres umbrales: la sección 18 escribe 8,5 km `PASS_MIN_KM`, 3.200 m `QUEEN_MIN_CLIMB_METRES` y «3 km del final en alto» (decisión 26 solo dice «8,5 / 3.200 / 3»). Confirmar con la sección 12 que 3.200 es `QUEEN_MIN_CLIMB_METRES` y cómo se llama el corte de 3 km en `stageKind.ts`.
- [18-dueno.md] D8, lista de países y ejemplos: la sección 18 toma de la sección 6 (borrador l. 106) que además de BE, NL, DK, AE, AU quedan sin reina NO, GB, PL, DE, JP, US, y da como ejemplo de rechazo posible «GB con `britanicas`, US con `norteamerica`» suponiendo que esas zonas tienen `relieve ∈ {montana, alta}`. Comprobar contra la tabla `ZONAS` de la sección 6 y, si no es así, quitar el ejemplo.
- [18-dueno.md] Registro de las respuestas: la sección 18 decide que las diez respuestas se anotan en un apartado propio de la nota «v61 · El generador es una gramática» de `balance.md` y que cada valor lleva el comentario `// Dn: valor por defecto (docs/generador.md §18)`. Alinear con el formato de nota de la sección 13 (previsión fallida) y con el paso 11 de la sección 15.
- [19-apendices.md] El recuento «45 injertos = 26 ideas distintas» (19.1.1) es un conteo mío sobre la tabla §E.1 agrupando duplicados entre jueces (V8 ×3, censo ×3, huella ×3, itinerario ×2, RACE_REGION ×3, nivel 2 ×2, km por clase ×2, paso 0/legacy ×2, etc.); la pasada de coherencia debería confirmar la cifra o quitarla.
- [19-apendices.md] En I-1 afirmo que el `puertoLargo` de geografia no existe como campo de `GeoSignature` y que lo cubren `finalesAlto: 'largo'` y el V4 de altitud; §B.2 no tiene `puertoLargo`, pero la sección 6 podría haberlo resuelto de otra forma.
- [19-apendices.md] El esqueleto dice que la sección 19 «cierra con la tabla de §E.2»; yo cierro con §E.2 (19.2.3) y añado después las hipótesis para la fase adversaria (19.2.4) y la tabla §E.3 de obligaciones (19.2.5). Si la cabecera (sección 0) ya reproduce §E.3, la pasada de coherencia decide si 19.2.5 sobra.
- [19-apendices.md] Las 217 líneas quedan por debajo de las 260 objetivo, aunque las tablas son largas (45 + 20 + 22 + 27 + 8 filas); si hace falta volumen, lo natural es ampliar 19.1.3 con un párrafo por injerto con cambio.
- [19-apendices.md] En 19.2.2 (lo que no se tomó) cito objeciones de los jueces a las no ganadoras con su §; no he vuelto a abrir las propuestas para verificar cada § citado por los jueces (por ejemplo banco §4.2 run-in 25 km, geografia §5.3 firmas), solo los juicios.
